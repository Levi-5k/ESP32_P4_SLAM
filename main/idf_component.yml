dependencies:
  # ESP32-P4 + ESP32-C6 SDIO WiFi support (required for dual-chip architecture)
  espressif/esp_wifi_remote: "*"
  espressif/esp_hosted: "*"
  
  # Camera components (from ESP-IDF examples)
  sensor_init:
    path: ../common_components/sensor_init
  
  idf:
    version: ">=5.3.0"
