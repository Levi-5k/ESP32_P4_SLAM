dependencies:
  # WiFi support disabled - SD storage conflicts resolved
  # espressif/esp_wifi_remote: "*"  # DISABLED
  # espressif/esp_hosted: "*"       # DISABLED
  
  # Camera components (from ESP-IDF examples)
  sensor_init:
    path: ../common_components/sensor_init
  
  idf:
    version: ">=5.3.0"
