#include "msp_protocol.h"
#include <string.h>
#include <esp_log.h>

static const char *TAG = "MSP_Protocol";

esp_err_t msp_protocol_init(void)
{
    ESP_LOGI(TAG, "MSP Protocol placeholder initialized");
    return ESP_OK;
}
